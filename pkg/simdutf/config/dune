(executable
 (name discover)
 (libraries dune.configurator))

(rule
 (targets
  flags.sexp
  c_flags.sexp
  c_library_flags.sexp
  c_flags.txt
  cxx_flags.sexp
  cxx_flags.txt)
 (action
  (run ./discover.exe)))
